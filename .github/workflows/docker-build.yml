name: Publish Docker Image

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    environment: Production

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Create .env File
        run: |
         # Assuming production environment variables are accessible as GitHub Actions secrets
         echo "DATABASE_URL=postgresql://postgres:ta81TSMaktITp5k5@db.lvvqrihhvntnibjwvbly.supabase.co:5432/postgres" >> .env
         echo "CHK=YES its work" >> .env

      - name: Install Dependencies
        run: yarn install

      - name: Build Application
        run: yarn build

      - name: Login to docker hub
        run: echo "${{secrets.DOCKERHUB_TOKEN}}" | docker login -u "${{secrets.DOCKERHUB_USERNAME}}" --password-stdin

      - name: Build Docker Image
        run: docker build . --file Dockerfile --tag ${{secrets.DOCKERHUB_USERNAME}}/ocean-harbor-server:latest

      - name: Push Docker Image
        run: docker push ${{secrets.DOCKERHUB_USERNAME}}/ocean-harbor-server:latest
